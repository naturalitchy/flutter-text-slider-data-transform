
import 'package:flutter/material.dart';

class SliderResultScreen extends StatelessWidget {
  const SliderResultScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}